# Historical WoT AV 0.1-proposed

Exact tracked tree at `bec38fcfc65960cf2f0ac6c0befd2b4eaabb5db2`, copied with `git archive` before the breaking 0.2 migration. The snapshot contains its original specification, tools, tests, release manifest and third-party notices, without a `.git` directory.

The uncommitted README and new research document were deliberately excluded. No ignored or private local-only tree was copied. Historical bytes must not be formatted, regenerated or resealed from the active draft.

See [the original-byte manifest](v0.1-proposed.original-manifest.json) and [the original specification](v0.1-proposed/spec.md). `python -B tools\archive_v01.py --check` checks every archived byte against the original SHA-256 and Git blob hashes.
